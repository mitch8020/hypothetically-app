# Claude Code Context: Worktree {WORKTREE_NAME}

## Quick Reference

| Property | Value |
|----------|-------|
| **Worktree** | `{WORKTREE_NAME}` |
| **Branch** | `{BRANCH_NAME}` |
| **Parent Branch** | `{PARENT_BRANCH}` |
| **Backend Port** | `{BACKEND_PORT}` |
| **Frontend Port** | `{FRONTEND_PORT}` |
| **Backend Path** | `{BACKEND_PATH}` |
| **Frontend Path** | `{FRONTEND_PATH}` |

## Current Task

{DESCRIPTION}

---

## Working Directory Context

You are working in a Git worktree that is separate from the main development directories.
This worktree can contain backend/frontend repos for a specific task branch.

### Branch Hierarchy

```text
<base-branch>
\-- {PARENT_BRANCH}
    \-- {BRANCH_NAME} (YOU ARE HERE)
```

### Important Rules

1. Commit only to `{BRANCH_NAME}`.
2. Open PRs to `{PARENT_BRANCH}` unless your team process says otherwise.
3. Do not push directly to protected base branches.
4. Keep changes scoped to the assigned task.

### Server Configuration

When running servers locally, use:
- Backend: `http://localhost:{BACKEND_PORT}`
- Frontend: `http://localhost:{FRONTEND_PORT}`

### Common Commands

```powershell
# Check status in both repos
cd "{BACKEND_PATH}" ; git status
cd "{FRONTEND_PATH}" ; git status

# Push current branch
git push origin {BRANCH_NAME}
```

### Sync Commands (from `worktrees/scripts/`)

```powershell
.\sync-from-development.ps1 {WORKTREE_NAME}
.\list-worktrees.ps1
```

### File Paths

- Backend: `{BACKEND_PATH}`
- Frontend: `{FRONTEND_PATH}`
- Config: `{CONFIG_PATH}`

---

## Workflow Reminders

1. Confirm scope and task before edits.
2. Sync your worktree branch before implementation.
3. Commit in small, meaningful units.
4. Run local checks before requesting review.
5. Clean up worktree after merge.

---

*Generated by review-worktree.ps1 on {GENERATED_DATE}*
